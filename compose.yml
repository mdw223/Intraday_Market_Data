services:
  redis:
    image: redis:latest
    container_name: imd_redis
    ports:
      - 6379:6379

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: imd_api
    env_file:
      - ./api/.env
    ports:
      - 3000:3000
    depends_on:
      - redis